#include "library.h"

Llibrary::Llibrary(QObject *parent):QObject (parent) {

    //Level::Level(int heroX, int heroY, unsigned int lives, unsinged int coins){
    // mainHero = new HeroObject(heroX, heroY, lives, coins);
    //}
//    EnemyObject* Level::addEnemy(int x,  int y){

//        EnemyObject* newEnemy = new EnenmyObject(x, y);
//        Enemy.push_back(newEnemy);
//        return newEnemy;
//    }

//    void Level::checkArea(unsigned int radius){
//        for(auto it: monsters)
//            if(1ull * (it -> getPosX() - mainHero -> getPosX())* (it -> getPosX() - mainHero -> getPosX()) +
//                    1ull*(it -> getPosY() - mainHero -> getPosY())* (it -> getPosY() - mainHero -> getPosY()) <= radius * radius)
//               it -> setCombatState(true);
//        else
//               it -> setCombatState(false);
//    }
  //  list <EnemyrObject*> Level::getListOfEnemies(){
    //    return enemies;
   // }

  //  HeroObject* Level::getMainHeroObject(){
  //  return mainHero;
}
